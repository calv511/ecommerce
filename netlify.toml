[build]
  command = "npm run build"
  publish = "dist"

# React Router handles routing client-side, so every path must serve index.html
# instead of 404ing on a direct visit or refresh of /cart or /profile.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
